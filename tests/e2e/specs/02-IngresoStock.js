describe('Test Ingreso de stock', () => {
    beforeEach(() => {
        // sessionStorage.usuarioLoggeado=true
        // sessionStorage.usuarioNombre='Cadorcha'
        // cy.visit('/')
        // cy.get('#BotonAbrirMenu').click()
        // cy.get('#IngresoStock').click()
    })

    it('Título principal debe ser "Ingreso de stock"', () => {
        // cy.get('#TituloMenuPrincipal]').contains("Ingreso de stock")
        // cy.wait(1000)
        // cy.get('#SelectorEmpresa').parent().click()
        // cy.get('.v-menu__content').contains("AREA TEST").click()
        // cy.get('#BarcodeLeido').type('111222').type('{enter}')
        // cy.contains('Barcode inexistente')
    });
})

